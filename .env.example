# Runtime knobs — copy to .env and adjust
# Configuration priority: CLI flags > .env > config.json

MAX_APPLICATIONS=10
HEADLESS=false
DRY_RUN=false
PLATFORMS=linkedin
LOG_LEVEL=info
DELAY_MIN_BETWEEN_APPS=5000
DELAY_MAX_BETWEEN_APPS=15000
SCREENSHOT_ON_ERROR=true
